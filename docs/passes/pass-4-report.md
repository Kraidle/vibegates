# Pass 4 — Report (open; updated per unit)

**Plan**: [pass-4-plan.md](pass-4-plan.md) · **Integrator**: orchestrator (Fable 5). This report accumulates unit evidence as the pass progresses; the close section lands with the final unit.

## U1 — D10 (CRA dates into templates) — DELIVERED

OJ-verified dates (register: [pass-3 adjudications §2](pass-3-adjudications.md)) carried into `templates/pass-report.md` (G6 row) and `templates/ci-gates.yml` (g6 job), EN + French corpus mirror: three staged application dates (Chapter IV 2026-06-11; Art. 14 reporting 2026-09-11 with the full ladder — 24 h early warning, 72 h notification, ≤ 14 days final report for vulnerabilities / ≤ 1 month for incidents; main body 2027-12-11), Annex I Part II (1) SBOM floor verbatim, ENISA SRP watch item with per-pass re-check instruction.

## U2 — D6 (guard extensions) — DELIVERED, live-fire evidence

Two new guards, five versions, four rejects, one pre-decided escalation honoured — the review history is the evidence the framework works:

- **v1 rejected** (G2 run `wf_d89d8ab9-cfd`: 2 blocking + 6 major + 13 minor, 24 reviewer fixtures): reproduced silent fail-open via an ADR-blessed invalid exclude pathspec (a one-line kill-switch), unredacted secrets printed into CI logs, prose false positives in `--tree` mode, non-matching accented French, lowercase-only generic patterns.
- **v2 rejected** (run `wf_8785e026-c44`): the hand-rolled diff parser introduced its own class — quoted-header fail-open on non-ASCII filenames (git's default `core.quotepath`), a `++`-content blind spot with **attacker-controllable path hijack**, line-number drift, unchecked `mktemp` fail-open, and a residual mode asymmetry.
- **v3 removes the class architecturally**: no diff parsing at all. Both modes run one scan function over a git file list — staged mode scans the **full staged blob** of every touched file (semantics declared: touching a file adopts its content), `--tree` scans tracked files; symmetry is structural, not promised. `grep -I` handles binaries (a bash variable cannot carry NUL bytes — v3.0's var-based binary test matched everything and was caught by fixture before review). The secrets guard never pipes matched content toward its output at all, making redaction structurally undefeatable; fail-CLOSED on every error path including `mktemp`.
- **v3 rejected too** (run `wf_c22e4ce6-921`; 8/8 v2 closures verified — then new classes of the v3 design itself): `grep -I` = a one-NUL-byte silent kill-switch (and every UTF-16 file unscanned), cwd-dependent scope narrowing, permanent block on submodule repos, the GIST guard leaking credentials through its own output, `//!` Rust doc comments blind.
- **v4 = terminal fixes + a written CONTRACT as the termination device** (advisor-guided after two failures on the same approach, per R-26): `tr -d '\0'` kills the NUL class and makes UTF-16 matchable; auto-`cd` to the repo root; symlinks/gitlinks skipped WITH NOTICE; both guards' output redacted (they share a CI stream); shebang-only `#!` guard; truncation announced. Each guard header now carries its G0 contract (covered scope, declared out-of-scope, remedies); **a G2 finding outside the contract is a contract-change proposal, not a defect** (R-23). **Escalation criterion, decided in advance: if the next review finds an in-contract fail-open, bash iteration STOPS and the question goes to an ADR under the maintainer's procuration** (gitleaks-wrapper dependency vs downgrading staged mode to advisory with CI as the sole blocker) — no silent v5.
- **v4 rejected — and the pre-decided ESCALATION CRITERION fired** (run `wf_5634fdb0-788`): one in-contract fail-open — gate-secrets hardcoded an undeclared `enforcement/` exclusion (the sibling guard declared the same exclusion; the omission survived three rounds because the acceptance set tested what the guard catches, never what it declines to look at). As pre-committed: bash iteration stopped and the question went to **[ADR-0004](../adr/ADR-0004-guard-escalation-adjudication.md)** under the maintainer's procuration, which adjudicated the root cause as a *declaration gap*, not an engine limit (gitleaks-wrapper and advisory-downgrade alternatives rejected with cause; the criterion stays armed for any true engine fail-open).
- **v5 = the ADR-0004 sanctioned set**: the exclusion declared AND announced at runtime (NOTICE parity — no silent scope removal, ever); the exclude-file escape hatch bounded (must be tracked; blanket pathspecs rejected; every applied exclusion announces its removed-file count; ADR- token documented as attribution, not validated cross-reference); GIST docstring/%-comment/REM blind spots declared; the **negative-space meta-fixture** added — a credential in any specially-treated directory must block or produce a naming NOTICE, so silent scope removal is now itself a failing fixture.
- **Round 5: accept-with-corrections** (run `wf_2e15e50b-99b`; engine probes all clean — UTF-16BE, PEM, 5 000-line files, special filenames — nothing escalation-triggering). Integrator-closed with fixtures: blanket-EQUIVALENT pathspec spellings (`.//` etc.) rejected via normalization + parent-traversal ban + empty-scope check; the gitlink fixture made discriminating (mutation-tested by the reviewer); GIST now announces its exclusions (NOTICE parity completed); version labels aligned. Round-5 proposals adopted: **unquoted dotenv/env-style assignments covered** (the dominant carrier in the Sakib population); the enforcement/ carve-out **narrowed to the guard files + tests/** (the rest of enforcement/ IS scanned — an adopter's enforcement/ is ordinary project space); a G2 checklist line obliges reviewers to validate the ADR named by any exclude-file diff.
- **The acceptance set is committed and third-party-reproducible**: `enforcement/tests/run-fixtures.sh` — **39/39 fixtures pass**, wired as a blocking own-repo CI job (`guards-acceptance-set`). The earlier uncommitted "17/17" count is superseded by this artifact (the reviewer was right that an uncommitted count is a second-hand number).
- **CI wiring**: real steps in `templates/ci-gates.yml` (g3 secret floor + dedicated-scanner backstop note; g5 GIST + flag-register + config-as-code steps) and the new **`templates/ci-recheck.yml`** scheduled full-lockfile recheck (weekly per register; tighter cadence ADR-settable given the measured ≥ 5-day stealth floor); own-repo dogfood: `g3-no-credentials` job (gate-gist deliberately not dogfooded — docs repo, asymmetry recorded in the workflow header). French mirror updated.

## U3 — D7 (R-8 four-layer dependency checker) — DELIVERED, live-fire evidence

`enforcement/check-dependency.sh` per **[ADR-0006](../adr/ADR-0006-r8-dependency-checker.md)** (contract-first — the U2 lesson applied): L1 existence (404 = hallucination suspect, block), L4 bundled universal-hallucination denylist (Churilov public subset — blocks even when the name resolves), L2 Levenshtein ≤ 2 in pure awk against bundled popular floors (ConfuGuard deployed thresholds cited), L3 cross-ecosystem warning (declared weak signal, never a block alone). Fail-closed on missing curl, uncovered ecosystems, bad charsets and every non-404 network failure. No LLM in the decision path. FP/FN declared UNMEASURED pending procurement 15. **Live fire against the real registries: 13/13** (`run-fixtures-r8.sh`, network-dependent, scheduled in `ci-recheck.yml`, never merge-blocking) — including the strongest case class: **four real squat-adjacent packages that EXIST on the registries (`pips`, `expresss`, `reacct`, `panda`) all blocked at L2 naming their popular target**, exact-match populars pass, the lev arithmetic is a COMMITTED fixture (3/1/0 — the uncommitted-count lesson applied).

**U3 G2 review** (run wf_f26ad67d-880): **accept-with-corrections** — 6 major + 4 minor + 4 proposals, all closed with fixtures: CRLF data class killed (tr -d CR + .gitattributes pinning LF — the U2 NUL lesson, same family); missing data files now FAIL CLOSED (ADR-0004 parity); **PEP 503 normalization both sides** (the reviewer proved google_auth bypassed a denylisted google-auth — variants are the same PyPI project); L4 finally exercised on a RESOLVING name via the scratch-copy fixture (the 10 shipped names all 404 today — the post-disclosure state, retitled honestly); every fixture now asserts the LAYER LABEL, not just the exit code; the 77.7 %/median-1 figures traced into the register; the two hallucinated @ember anchors removed from the npm popular floor (denylist-and-floor disjointness is now itself a fixture); L3 got a machine-visible exit code 1 — and its own live-fire coverage. **Final: 22/22 network fixtures.**

## Remaining units

U4 (hooks pack, ADR-0003) · U5 (own R-25 bound, ADR-0005) · U6 (oracles, headless template, plugin pack) · transverse (MAST→checklist, doc 05 revision). Debt register unchanged otherwise; close section pending.
