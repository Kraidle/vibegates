# Pass 2 — Traced improvement backlog (S2)

**Status**: adjudicated 2026-08-13 by the integrator from the 11-worker source sweep (run `wf_d7c4b2d0-940`, workers pinned `claude-opus-5`, resolution-controlled). Every item carries its worker and source. Ranking: machine-enforceable first (the product's measured gap: 26 rules, 2 machine-enforced before this pass — R-13, R-22; after it, R-1/R-4 join them on this repository via one linter, and R-25 ships template-side for adopters, own-repo wiring being debt D1). Items marked **P2** shipped this pass; **P3**/**P4** are scheduled, not vague: each appears in the pass-report debt section with owner and due pass.

## Tier 1 — machine-enforceable, high yield

| # | Item | Basis (worker → source, level) | Status |
|---|---|---|---|
| 1 | **Model-pinning linter** over `.claude/agents`, `.claude/skills`, settings JSON; ADR-annotated exemptions only | A2 → sub-agents doc [read] (`model:` accepts full IDs; frontmatter pin outranked by `CLAUDE_CODE_SUBAGENT_MODEL` and per-call param — so lint AND first-launch resolution control, never lint alone) | **P2 shipped**, live-fire 3 rounds / 23 fixture runs (G2 v1 caught 2 real defects; fixed, re-proven) |
| 2 | **Pre-commit secret scan** added to the commit guards | L1 → Sakib et al., KDD-AgenticSE 2026 [abs]: 99.6 % of critical-severity smells in agent PRs are hard-coded credentials; 81.1 % survive review; humans introduce 67.6 % | P3 (guard extension, same mechanism as R-13 guard) |
| 3 | **GIST detector**: extend the TODO/FIXME guard to AI-reference + uncertainty markers ("not sure", "need to test" near Copilot/Claude/Gemini refs) | L4 → TechDebt 2026 (GIST), DOI 10.1145/3794915.3795777 [abs] | P3 (regex extension of existing guard) |
| 4 | **R-8 registry-check script, 4 layers**: existence → Levenshtein ≤ 2 proximity to more-popular package (≥10× downloads, floor 5 000/wk) → cross-ecosystem → seeded denylist; Python stricter than JS | L5 → Churilov 2026 replication [read] (frontier rates 4.62–6.10 %; 8/10 top hallucinated names within distance 2 of real packages); ConfuGuard [read] (deployed thresholds; FP 80 %→28 %; LLM must never be the load-bearing classifier — accuracy fell 0.83→0.68) | P3 (spec ready; publish precision/recall as its own evidence once measured) |
| 5 | **Scheduled lockfile re-verification** (weekly CI), not install-time-only | L5 → ConfuGuard [read]: 13.3 % of npm confusion attacks inject malware ≥ 5 days after release | P3 (one CI cron job) |
| 6 | **FAIL_TO_PASS witness required in CI**: every behaviour-changing PR must show ≥1 test failing pre-change and passing post-change | L2 → Wang/Pradel/Liu (SWE-bench correctness) [read]: 29.6 % of test-passing patches diverge behaviourally; resolution rates inflated ~6.4 pts | P3 |
| 7 | **Two-oracle G3**: dev suite agent-visible; acceptance suite outside the agent's workspace, CI-injected; fail if diff touches acceptance paths | L2 → "Building to the Test" 2026 [abs] (agents deliver what you check); L1 → BaxBench ICML 2025 [abs] (~50 % of functionally correct backends exploitable → separate security oracle, ≥2 detectors per Dai ICSE 2026 [abs]) | P3 |
| 8 | **Safety monotonicity check**: security-oracle result diffed before/after any agentic refinement loop; regression blocks | L1 → SCAFFOLD-CEGIS [abs] + Shukla [abs] (naked refinement degrades security; SAST-only gating worsened latent degradation 12.5 %→20.8 % — SAST is a floor, never the ceiling) | P4 (needs exploit-oracle availability; cost recorded as open question) |
| 9 | **Hooks pack** (plugin-distributable): PreToolUse commit guard (exit 2, matcher `Bash\|PowerShell`, short timeout — a timed-out command hook FAILS OPEN), ConfigChange guard on `disableAllHooks`/hook-stripping, SessionStart gate-digest re-injection (survives compaction; ≤10 k chars), Stop-hook debt check (soft: overridden after 8 consecutive blocks — CI remains the backstop) | A1 → hooks reference [read] — incl. the T1/T2 tiering (repo hooks developer-defeasible; only managed settings non-defeasible) | P3 (ADR-0003 carries the full design) |
| 10 | **Provenance auto-logger**: PostToolUse async logger on `Edit\|Write\|NotebookEdit` + SessionStart {session_id→model} sidecar join; record `model_id_source: seen\|absent` | A1 → hooks reference [read]: **model id is NOT in any per-tool-call payload; no $CLAUDE_MODEL** — "log the model on PostToolUse" is unimplementable as stated; session-scoped attribution is the honest maximum (transcript-schema procurement filed) | P3 (ADR-0003) |
| 11 | **Headless AI gate template**: `claude -p --bare --model <full ID> --permission-mode dontAsk --tools "Read,Grep,Glob" --json-schema <verdict> --max-turns --max-budget-usd --strict-mcp-config --no-session-persistence`; PASS/FAIL parsed from `structured_output`, never exit code; fail on non-empty `mcp_server_errors`; never `auto` in CI (classifier blocks don't stop `-p` runs) | A3 → headless/CLI/permission-modes/permissions docs [read]; empirical flag-combination test required before publishing (doc warns not every option combines with `-p`) | P3 |
| 12 | **Permission-template doctrine**: repo ships deny/ask only (committed allow rules are trust-gated and ignored under `-p`); deny `Edit(.github/workflows/**)`, `Edit(.claude/**)`, `Bash(* --no-verify *)`; ban environment-runner wildcards (`npx *`, `docker exec *` = full shell escape); managed-settings hardened baseline + sandbox trio (`failIfUnavailable` etc.) as `templates/` | A3 → permissions/sandboxing docs [read]; "a repository can restrict itself but never widen itself" | P3 |
| 13 | **Feature-flag debt gate**: flag without owner+type+removal condition = naked debt (blocks like a TODO); removal-lag as CI metric; per-project expiry from own median lifespan | L7 → Rahman MSR 2016 [read] (only 20 % of Chrome release toggles removed; 17 % lingered); Ternava 2026 [abs] (removal lag 13–35 %; medians 734 vs 185 days — no universal constant); SoCC 2022 [read] (wrongly-set flags = 24.4 % of code-bug incidents) | P3 |
| 14 | **Config = code**: config and flag changes pass G2/G3 like code | L7 → SoCC 2022 [read]: 60 % of severe incidents non-code; config bugs 12.5 %, deployment errors 13.2 % | P3 (one rule sentence + CI paths) |
| 15 | **SKILL.md / plugin CI lint**: `claude plugin validate` in CI + frontmatter hard limits (name ≤64, description ≤1024, body <500 lines, refs one level deep) | A2 → skills/plugins docs + authoring best practices [read] | P3 (ships with the .claude pack) |
| 16 | **R-25 batch-size job** (fail-closed until ADR-set limit) | DORA 2024 [read], already in matrix; release-side extension per Savor ICSE-C 2016 [read] (median 33 changed lines/deploy at Facebook; critical failures ~flat across 5–35 k deploys/month) | **P2 shipped** (template); own-repo wiring P3 |

## Tier 2 — gate/doc revisions forced by evidence (falsifiability clause 02 §5)

- **DORA row must be split** — throughput: 2024 negative, **2025 positive** (not robust; stop citing a throughput penalty); stability: negative in 2024 AND 2025 (**replicated** — promote to the framework's strongest longitudinal claim; mechanism: "time saved writing is re-spent auditing"). [L4 → DORA 2025 announcement, read]
- **Duplication row must be split** — ecosystem-temporal (GitClear 2026 report: duplicated lines 40.3→73.0 per M changed lines, 2023→2026 YTD; moved code 21 % of changed lines in **2022** → 3.8 % in 2026 YTD; legacy maintenance −74 % vs 2022) vs within-project comparative (Mao 2026: AI files LESS duplicated than human files, 18.69 % vs 25.89 %). Note: README currently cites "~25 % (2021)" for the same moved-code series from the earlier GitClear 2025 report — **distinct start years of one series, not a transcription conflict**; the D3 edit states both with their years or keeps only the 2026-report pair. "AI code is more duplicated than human code" is **unsupported and must be struck**; the defensible claim is workflow-level. [L4, both read]
- **Veracode row refreshed**: 2026 report holds the stationary ~44–45 % (56 % pass vs 55 % first report; 100+ models, 4 years; best model 68 %); per-CWE variance extreme (XSS 15 % pass, log injection 12 %) → G3 detectors CWE-weighted; Java lowest (30 %). [L1 → Veracode 2026, abs, grey]
- **SATD-LLM row updated in place, not duplicated**: the existing citation is arXiv:2601.06266, now venue-confirmed **SANER 2026** (v3, 2026-01-20) — row rises from preprint to peer-reviewed; new figures available (SATD accumulation 3.95 % LLM vs 4.10 % ML; median debt-free interval 492 days — an empty debt register in a young repo is uninformative for G5). [L4, abs]
- **REJECTED RULE, recorded so it is not re-proposed**: mandatory security prompting — no statistically significant effect (all χ² p>0.05, 16 000 files), CWE-hint prompting made things worse in nearly all configs. Security is verified at the artifact, never requested at the prompt. [L1 → arXiv:2605.24298, read]
- **Leaderboard scores inadmissible** as sole basis of a model/agent-selection ADR (contamination: 76 % vs 53 % file-path identification in vs out of SWE-bench). [L2 → SWE-bench Illusion, abs]
- **G2 anchored to standards**: NIST SP 800-218A §1.2 (no AI-exemption for review — verbatim anchor) + SLSA v1.2 Source L4 (two-person review). G1→G6 provenance ladder = SLSA v1.2 Build L1/L2/L3, version-pinned (v1.1 is now Retired — cite versions or drift silently). CDXA as machine-readable gate-verdict format; ML-BOM for the model-pinning record. [L5 → NIST/SLSA/CycloneDX/PEP 740, read]
- **CRA dated checkpoint**: reporting obligations apply **2026-09-11** (four weeks away); main obligations 2027-12-11. OJ legal text procurement filed (EUR-Lex unreachable; mirror text stays [2nd], not quotable). [L5, read (Commission policy page)]
- **100× cost-of-change claim banned from our own rhetoric**: Boehm & Basili 2001 themselves say 5:1 for small non-critical systems; Menzies 2017 (171 projects) finds no delayed-issue effect (max ratio 3.0); the "IBM Systems Sciences Institute" table has no retrievable study behind it. Upstream gates are justified by irreversibility/blast-radius/compliance — never by the curve. [L6, all read]
- **G2 review is cognitively redesigned** (see lifecycle doc §G2): blind-first ordering (Buçinca CSCW 2021 [read]: explanations INCREASED agreement with wrong output, 0.64 vs 0.49 no-AI); AI-hunk labelling in the diff (Tang 2024 [read]: repair success 0.577 vs 0.446 when provenance disclosed, p=.0417); three-level multi-file workflow (Heander 2026 [abs], design hypothesis); review payload sanitised of self-assessment (EACL 2026 [read]: "this is correct" comments move judges up to +26.4 pts).
- **R-21 sharpened**: exactly ONE adversarial verification pass per artifact version (multi-turn review degrades: precision 0.30→0.20, +62 % false findings); reviewer runs context-separated (artifact + spec only — a subagent inheriting the prompt did no better than self-review); external feedback (tests) before LLM review (ICLR 2024: intrinsic self-correction fails); no consensus/vote/debate mechanisms (ICML MAS 2025: agents talk correct peers out of right answers); reviewer precision (confirmed/raised) recorded per review. **Honest basis**: R-21/R-26 are justified as "removes a known failure mode at low cost", NOT "verified correctness improvement" — the positive evidence is one single-author preprint harness. [L2]
- **Anti-metric extensions**: throughput/velocity evidence inadmissible for gate waivers in either direction (Cui MS 2026 +26 % tasks vs METR −19 %→intervals straddling zero, measurement collapsed under selection bias); team-level DORA aggregates inadmissible for unit health (only 26.8 % of services track their team aggregate; one service = 71.9 % of outages under an acceptable aggregate). [L3 → Cui/METR; L7 → Rueegger ICSSP 2024, all read]

## Tier 3 — agent-organization pack (P3 build, design fixed this pass)

`.claude/` pack distributed as a **plugin** (collision-proof namespace, explicit semver — commit-SHA versioning would change gates under teams silently):
- **Agents**: `vg-reviewer` (G2 — `tools: Read, Grep, Glob`, NO `memory` field: declaring memory silently re-enables Write/Edit), `vg-librarian` (doc-03 sourcing), `vg-auditor` (G5/G6), `vg-redteam` (G7), all `model: claude-opus-5`, `effort: high`; advisor stays a user-scope agent. Plugin agents cannot carry `hooks`/`permissionMode` → per-agent enforcement moves to session-wide hooks in the plugin's `hooks/hooks.json`.
- **Skills**: `vg-pass-open`, `vg-review-checklist` (`context: fork`, `agent: vg-reviewer`, `background: false` — the checklist body becomes the clean-room reviewer's prompt), `vg-provenance-entry`, `vg-pass-close`; side-effecting skills get `disable-model-invocation: true` (human-triggered only — and therefore can never be preloaded; reference skills stay model-invocable). Bodies are standing invariants, not step lists (content persists all session, never re-read).
- **Evals**: ≥3 scenarios per gate skill + a small runner script (the vendor's own bar — and the vendor states no built-in runner exists; we ship one).
- **SubagentStart hook** appends agent_type + run metadata to the provenance journal; PreToolUse guard denies `git commit/push` when `agent_id` present (machine form of "a worker never commits", R-19/R-20); `CLAUDE_CODE_MAX_SUBAGENT_SPAWN_DEPTH=2`.
- **Known residual, declared**: no independent evidence that role-specialised agents improve review outcomes vs one general agent — every source is vendor-normative. The pack ADR records this as a design bet with the R-21 evidence asymmetry note. Agent-teams doc (unexplored coordination primitive) evaluated before the pack freezes.
